"""Compatibility layer for the historic `email_automation` package.

This repository references an `email_automation` engine that may live in a
separate sibling repo in some deployments. In this repo we ship a minimal
implementation so Django can start without external layout assumptions.
"""

from __future__ import annotations

